name: Deploy to Production

on:
  push:
    branches: [main, master]
  workflow_dispatch:  # Permite ejecución manual

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production  # Esto conecta con el environment de GitHub
    
    env:
      API_URL: ${{ secrets.API_URL }}
      API_KEY: ${{ secrets.API_KEY }}
      NODE_ENV: production
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci  # Usa ci para instalación limpia
      
      - name: Run tests (si tienes)
        run: npm test
      
      - name: Deploy
        run: |
          echo "🚀 Desplegando aplicación..."
          echo "🔗 API URL: $API_URL"
          echo "📦 NODE_ENV: $NODE_ENV"
          
          # Ejemplo usando los secrets sin exponerlos
          echo "✅ Configuración cargada correctamente"
          
          # Tu comando de deploy real aquí
          # Por ejemplo: npm run deploy