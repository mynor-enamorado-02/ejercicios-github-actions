name: Deploy to Production

on:
  push:
    branches: [main, master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    
    env:
      API_URL: ${{ secrets.API_URL }}
      API_KEY: ${{ secrets.API_KEY }}
      NODE_ENV: production
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build (si tienes script build)
        run: |
          if grep -q '"build"' package.json; then
            npm run build
          fi
      
      - name: Deploy
        run: |
          echo "✅ Secrets configurados correctamente"
          echo "🌐 API_URL está configurada"
          echo "🔐 API_KEY está configurada (no visible)"
          # Aquí iría tu comando real de deploy